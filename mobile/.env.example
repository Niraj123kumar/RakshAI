# GigShield Mobile — Environment Variables
# Copy to .env (never commit .env)

# API URL — change per environment
EXPO_PUBLIC_API_URL=http://localhost:8000

# For production:
# EXPO_PUBLIC_API_URL=https://api.gigshield.in
